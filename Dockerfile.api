FROM python:3.10

WORKDIR /app

RUN pip install pipenv

COPY Pipfile Pipfile.lock* ./

RUN pipenv install --system

COPY . .

CMD ["uvicorn", "src.router:app", "--host", "0.0.0.0", "--port", "70"]
